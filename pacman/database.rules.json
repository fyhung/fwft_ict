{
  "rules": {
    ".read": false,
    ".write": false,
    "rooms": {
      "$roomId": {
        ".read": "auth != null",
        ".write": "auth != null && ((!data.exists() && newData.child('meta').child('hostUid').val() == auth.uid) || data.child('meta').child('hostUid').val() == auth.uid)",
        "joinRequests": {
          "$uid": {
            ".write": "auth != null && auth.uid == $uid && root.child('rooms').child($roomId).child('meta').child('status').val() == 'lobby' && root.child('rooms').child($roomId).child('meta').child('joinLocked').val() == false",
            ".validate": "newData.hasChildren(['name', 'colorId', 'requestedAt']) && newData.child('name').isString() && newData.child('name').val().length > 0 && newData.child('name').val().length <= 16 && newData.child('colorId').isString() && newData.child('colorId').val().length == 3"
          }
        },
        "players": {
          "$uid": {
            "presence": {
              ".write": "auth != null && auth.uid == $uid && data.parent().child('seatId').exists()",
              ".validate": "newData.hasChildren(['online', 'lastSeenAt']) && newData.child('online').isBoolean()"
            },
            "lobby": {
              "ready": {
                ".write": "auth != null && auth.uid == $uid && root.child('rooms').child($roomId).child('meta').child('status').val() == 'lobby'",
                ".validate": "newData.isBoolean()"
              }
            }
          }
        },
        "inputs": {
          "$uid": {
            ".write": "auth != null && auth.uid == $uid && root.child('rooms').child($roomId).child('players').child($uid).exists() && root.child('rooms').child($roomId).child('meta').child('status').val() == 'playing'",
            ".validate": "newData.hasChildren(['seq', 'direction', 'clientTime']) && newData.child('seq').isNumber() && newData.child('seq').val() >= 0 && newData.child('clientTime').isNumber() && (newData.child('direction').val() == 'up' || newData.child('direction').val() == 'down' || newData.child('direction').val() == 'left' || newData.child('direction').val() == 'right' || newData.child('direction').val() == 'none')"
          }
        }
      }
    }
  }
}
